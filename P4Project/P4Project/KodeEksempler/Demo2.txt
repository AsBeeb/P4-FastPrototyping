global string hejStr = "Hej " : " med " : " dig!";
global float fsa = 2.0;

func void main()
{
	local int[] k;

	if(true)
	{
		local int newValue = 2222;
		local int anotherVal = k[2];
	} 
	else
	{
		local string minString = myFunc(20.2 , 3);
	};

	play (player vs opponents in k)
	{
		player = opponents[2];
	} until (true);

	local player MyPlayer = player(10);
}

func string myFunc(float test, int type)
{
	local string nyStr = "Hej Hej";
	local int a = test + 2;
	return nyStr;
}

struct player
{
	local int Field = 10;

	player (int field)
	{
		Field = field;
	}

}

