global string hejStr = "Hej " : " med " : " dig!";
global float fsa = 2.0;

struct person{
	local string navn;
	local int alder;
	local float hoejde = 150.5;
}

struct hund {
	local string navn = "Hunni";
	local int alder;
	local string race;
	local string battleaxe;
	hund(int Alder, string RACe){
	alder = Alder;
	race = RACe;
	}
}

func void main()
{
	local int[] k;
	local hund Fiddo;
	local float hejhej = GetNumber();
	Print(hejhej);

	local hund Viggo = hund(7, "Kokker");
	local person Michael;
	local hund[] hundeliste;

	if(true)
	{
		local int newValue = 2222;
	} 
	else
	{
		local string minString = myFunc(20.2 , 3);
	};

	play (player vs opponents in k)
	{
	local hund Fiddo2;
	} until (true);

	local player MyPlayer = player(10);
}

func string myFunc(float test, int type)
{
	local string nyStr = "Hej Hej";
	local int a = test + 2;
	return nyStr;
}

struct player
{
	local int Field = 10;

	player (int field)
	{
		Field = field;
	}

}

